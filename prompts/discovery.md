# Structured game discovery

Discovery is now the schema-validated Concept Blueprint protocol. Start with `aigame concept start`, complete the bounded prompts under `prompts/concept/`, classify assumptions and risks, and stop at human gates. Only after finalization should the approved roadmap expose the smallest falsifiable experiment.
