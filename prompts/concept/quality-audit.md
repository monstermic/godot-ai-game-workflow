# Quality and roadmap task

Classify every item in every active profile as `required`, `optional`, or `not_applicable` with rationale and scope links. Do not turn every profile quality into mandatory scope. Produce a release-blocking `GDD-` contract covering every must mechanic/content entry and an acyclic `RMP-` whole-game roadmap. Only its first milestone may be task-detailed. Keep subjective claims bound to human playtest evidence.
