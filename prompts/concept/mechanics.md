# Mechanics task

Enumerate every launch mechanic with a stable `MEC-` ID. Define player intent, inputs, preconditions, outputs, states, transitions, processing order, rules, formulas, units, defaults, limits, tuning ranges, timing, probability, cost, rewards, interactions, stacking, priority, cancellation, immunities, conflicts, NPC use, edge cases, feedback, persistence, determinism, debugging, acceptance tests, objective criteria, and playtest hypotheses. Return a JSON `mechanics` array conforming to `mechanic-spec.schema.json` with no placeholders.
