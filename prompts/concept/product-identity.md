# Product identity task

Expand the selected pitch into a final proposed title, subtitle, short and long descriptions, package slug, executable/display name, save namespace, edition, branding vocabulary, naming rules, and store metadata. Keep externally assigned store identifiers explicitly external. Return JSON matching `product-identity.schema.json`; title and identity require exact human approval.
