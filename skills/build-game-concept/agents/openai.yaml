interface:
  display_name: "Build Game Concept"
  short_description: "Create a complete implementation-ready game blueprint"
  default_prompt: "Use $build-game-concept to turn this game idea into a complete blueprint, following the repository automation mode."
policy:
  allow_implicit_invocation: true
