# Schemas

Canonical JSON Schemas are packaged in `src/aigame/schemas` and copied into every generated repository under `.aigame/schemas`. Keeping the installable copy canonical prevents the CLI, MCP server, and generated snapshot from drifting.
