# Capability packs

V1 implements `core-2d`. The other manifests are stable extension boundaries, not claims that their delivery pipelines already exist. Activating one requires a workflow upgrade, capability detection, contract tests, and human approval.
