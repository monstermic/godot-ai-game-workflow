# Codex adapter

The target repository's root `AGENTS.md` and `.aigame/automation.json` are authoritative. Use `build-game-concept`, then `generate-game-assets`, before implementation. The media skill must cover every blueprint mechanic, content entry, state, event, stage, ending, UI, and accessibility requirement. In `human_gated` mode stop for approvals; in `ai_staging` mode retain CLI-generated agent approvals and continue. Use TDD, fresh verification, and PRs targeting `staging`. Auto-approve ordinary project-local operations, but never bypass dependencies, paid providers, model downloads, independent review, the exact staging-owner approval, production, release, rollback, secrets, or destructive external changes.
