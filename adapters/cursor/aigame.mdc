---
description: Portable AI game workflow
alwaysApply: true
---

Read and follow `AGENTS.md` and `.aigame/automation.json`. Complete concept state, then follow `aigame assets next --json` through exhaustive planning, sampling, deterministic generation, validation, and integration. Human-gated mode stops for approval; AI-staging mode records agent approvals and continues. Keep dependency/provider/model gates protected, target `staging`, and use only the exact-approval-bound staging integration path.
