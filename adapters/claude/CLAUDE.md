# Claude Code adapter

Read and follow the target repository's `AGENTS.md` and `.aigame/automation.json`. Treat project and JSON records as canonical. Complete concept tasks before implementation and validate every result. Human-gated mode stops at approvals. AI-staging mode makes creative decisions automatically, records agent approvals, and continues through ordinary project operations toward a PR based on `staging`. Never bypass blocked results, independent review, the exact staging approval, production, release, rollback, secrets, paid services, or destructive external changes.
