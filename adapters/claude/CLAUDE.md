# Claude Code adapter

Read and follow the target repository's `AGENTS.md` and `.aigame/automation.json`. Treat project and JSON records as canonical. Complete the concept and then the `aigame assets` state machine before implementation; validate exhaustive blueprint coverage, deterministic recipes, runtime resources, licenses, provenance, and hashes. Human-gated mode stops at approvals. AI-staging mode records agent approvals and continues through ordinary project operations toward `staging`. Never bypass blocked results, dependencies, providers, model downloads, independent review, the exact staging approval, production, release, rollback, secrets, paid services, or destructive external changes.
