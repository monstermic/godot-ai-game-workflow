# Claude Code adapter

Read and follow the target repository’s `AGENTS.md`. Treat `.aigame/project.toml` and JSON records as canonical, use the `aigame` CLI for state changes, and stop when a command returns `needs_human` or `blocked`.
