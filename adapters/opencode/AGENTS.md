# OpenCode adapter

Load the global `build-game-concept` skill for concept work and `generate-game-assets` for visual/audio planning, generation, validation, integration, audit, or resume work. The target repository's root `AGENTS.md`, `.aigame/automation.json`, pinned schemas, and command results remain authoritative.

Skill activation and exact local `aigame concept *` and `aigame assets *` commands may be auto-approved. In `ai_staging` mode, use the global `ai-staging` profile, record agent approvals, and continue ordinary local operations. Target `staging`; obtain but never post the exact staging-owner approval comment. Dependencies, paid providers, model downloads, production, release, rollback, secrets, and destructive external operations remain protected.
