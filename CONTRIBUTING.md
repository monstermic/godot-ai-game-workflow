# Contributing

Open an issue for intent, then submit a focused PR. New behavior requires a failing test observed before implementation. Update schemas and fixtures together when a public contract changes. Run Python tests and the Godot headless suite before requesting review.

Do not add provider-specific policy to adapters, unpinned Actions, generated media without provenance, Git LFS objects to this template repository, or release logic that rebuilds after candidate approval.
